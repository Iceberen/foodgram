volumes:
  pg_data:
  static:
  media:
  redoc:

services:
  db:
    image: postgres:13
    volumes:
      - pg_data:/var/lib/postgresql/data
    env_file: ../.env
  backend:
    image: iceberen/foodgram_backend
    restart: always
    env_file: ../.env
    depends_on:
      - db
    volumes:
      - static:/backend_static
      - media:/app/media/
      - redoc:/app/api/docs/

  frontend:
    image: iceberen/foodgram_frontend
    restart: always
    volumes:
      - static:/frontend_static
    command: cp -r /app/build/. /frontend_static/
    env_file: ../.env
  gateway:
    image: iceberen/foodgram_gateway
    restart: always
    ports:
      - 8000:80
    volumes:
      - redoc:/usr/share/nginx/html/api/docs/
      - static:/staticfiles/
      - media:/app/media/
    env_file:
      - ../.env
    depends_on:
      - frontend
      - backend